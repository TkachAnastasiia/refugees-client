import './App.css';
import { Container } from '@mui/material';
import Router from './router';

function App() {
  return (
    <Container maxWidth={false}>
      <Router />
    </Container>
  );
}

export default App;
